const quizManager = require("../businessLayer/manager/quizManager");

const quizManager = new quizManager();

class quizService {
  // async getUserDetails(req) {
  //   return quizManager.getUserDetails(req.body.userEmail);
  // }
}

module.exports = quizService;
